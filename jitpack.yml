install:
  - mkdir target
  - ./jbang --fresh export --mavenpublish -O target -Dgroup=$GROUP -Dartifact=$ARTIFACT -Dversion=$VERSION hello.java
  - ./jbang --fresh export --mavenpublish  -Dgroup=$GROUP -Dartifact=$ARTIFACT -Dversion=$VERSION hello.java

